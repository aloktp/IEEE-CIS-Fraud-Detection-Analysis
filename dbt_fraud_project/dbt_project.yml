name: 'dbt_fraud_project'
version: '1.0.0'
profile: 'dbt_fraud_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_fraud_project:
    staging:
      +materialized: table
      +schema: STAGING
    intermediate:
      +materialized: table
      +schema: STAGING
    features:
      +materialized: table
      +schema: FEATURES
